# Railway Environment Variables Template
# Copy this to Railway dashboard and set values

ENV=production
LOG_LEVEL=INFO
CORS_ORIGINS=https://your-domain.com
RATE_LIMIT_RPM=60
CACHE_TTL_DAYS=30
REDIS_URL=redis://your-redis-url
DISABLE_LLM=true

# Port is automatically set by Railway via $PORT

